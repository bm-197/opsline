DATABASE_URL=postgres://opsline:opsline@localhost:5432/opsline

# Better Auth. Generate the secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Inngest cloud (local dev uses the dev server and does not need these)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
